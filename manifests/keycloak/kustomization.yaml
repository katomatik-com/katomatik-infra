apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The Keycloak instance (CR) plus the KSOPS-decrypted keycloak-db Secret it
# references. Realm import will be added here next.
resources:
  - keycloak.yaml
generators:
  - ./ksops-generator.yaml
