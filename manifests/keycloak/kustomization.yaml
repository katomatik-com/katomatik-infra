apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Generates the KSOPS-decrypted keycloak-db Secret. The Keycloak CR + realm
# import will be added here next (as `resources:`).
generators:
  - ./ksops-generator.yaml
